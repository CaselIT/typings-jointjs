{
  "private": true,
  "scripts": {
    "build": "echo building... && typings bundle -o out",
    "build-global": "echo building global... && cd global && typings bundle -o out"
  },
  "devDependencies": {
    "typescript": "^1.8.9",
    "typings": "^1.3.2"
  }
}
